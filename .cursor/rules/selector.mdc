---
description: Selector system — dynamic event kwargs (RandomListSelector, ValueSelector, StatSelector, ConditionSelector, nested selectors). Read wiki/Selectors.md before adding or wiring selectors.
globs: game/scripts/selector.rpy
alwaysApply: false
---

# Selectors

Canonical guide: **`wiki/Selectors.md`**. Read it before adding, nesting, or explaining selectors. Values land in event kwargs and in `<key>` image placeholders.

## Related wiki

- How events consume kwargs: `wiki/Events.md`
- Image substitution: `wiki/Images.md`
- Conditions used inside selectors: `wiki/Conditions.md`
- What `LevelSelector(..., "school")` means in play: `wiki/School-Levels.md`

## Code

- `game/scripts/selector.rpy` — all selector classes + `SelectorSet`
- `game/scripts/event.rpy` — runs selectors into kwargs
- `game/scripts/images.rpy` — `<key>` substitution
